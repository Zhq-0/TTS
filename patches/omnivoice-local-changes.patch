diff --git a/.gitignore b/.gitignore
index 510818d..5cf9cd7 100644
--- a/.gitignore
+++ b/.gitignore
@@ -20,6 +20,8 @@ build/
 /download
 /local/
 /run*
+/pretrained_models/
+/outputs/
 example.py
 results/
 examples/data*
@@ -28,3 +30,4 @@ examples/exp*
 .claude/
 *.wav
 *.jsonl
+!/evaluation/smoke_test.jsonl
diff --git a/omnivoice/training/builder.py b/omnivoice/training/builder.py
index 4af2fc7..c1ab81d 100644
--- a/omnivoice/training/builder.py
+++ b/omnivoice/training/builder.py
@@ -52,6 +52,11 @@ from omnivoice.training.config import TrainingConfig
 logger = logging.getLogger(__name__)
 
 
+def _sample_length(sample):
+    """Return a sample length using a Windows-spawn-picklable function."""
+    return sample["length"]
+
+
 def build_model_and_tokenizer(
     config: TrainingConfig,
 ) -> Tuple[OmniVoice, AutoTokenizer]:
@@ -175,7 +180,7 @@ def build_dataloaders(
             max_length=config.max_sample_tokens,
             max_sample=config.max_batch_size,
             processor=processor,
-            length_fn=lambda s: s["length"],
+            length_fn=_sample_length,
         )
         collate_fn = PaddingDataCollator(processor, config.batch_tokens)
 
@@ -222,7 +227,7 @@ def build_dataloaders(
                 max_length=config.max_sample_tokens,
                 max_sample=config.max_batch_size,
                 processor=processor,
-                length_fn=lambda s: s["length"],
+                length_fn=_sample_length,
             )
         eval_loader = DataLoader(
             dev_dataset,
diff --git a/pyproject.toml b/pyproject.toml
index d5f497f..547fd44 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -49,6 +49,10 @@ eval = [
     "jiwer==3.1.0",       # WER
     "s3prl",               # Speech representation (HuBERT etc.)
     "funasr",              # ASR models
+    "openai-whisper==20231117", # ASR metric used by the existing CosyVoice reports
+    "opencc-python-reimplemented==0.1.7", # Chinese text normalization
+    "pypinyin==0.55.0",    # Chinese pronunciation metrics
+    "jieba==0.42.1",       # Chinese word segmentation
     "zhconv",              # Chinese character normalization
     "zhon",                # Chinese punctuation
     "unidecode",            # Unicode normalization
@@ -89,6 +93,9 @@ constraint-dependencies = [
     "torchaudio==2.8.0",
 ]
 
+[tool.uv.extra-build-dependencies]
+openai-whisper = ["setuptools<81"]
+
 [tool.hatch.build.targets.sdist]
 include = ["omnivoice"]
 
